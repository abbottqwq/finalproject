import { url } from "./config";
const urls = {
	selectAll: `${url}selectAll`,
	selectByTimeAndComp: `${url}selectByTimeAndComp`,
	selectByTime: `${url}selectByTime`,
	selectByComp: `${url}selectByComp`,
	selectCompanyName: `${url}selectCompanyName`,
	selectTimePeriod: `${url}selectTimePeriod`,
	test: `${url}test`,
	initData: `${url}initdata`,
};
export default urls;
